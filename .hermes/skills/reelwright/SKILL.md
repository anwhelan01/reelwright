---
name: reelwright
description: Turn a topic into a scene-bound vertical script, then a shot list. One still per spoken line.
version: 1.0.0
metadata:
  hermes:
    tags: [k3ss, reelwright]
    category: desk
---

# Reelwright

Scene-bound shorts from a single line. Script, stills, voice, cut — in the browser.

## When to use

The operator is working in `reelwright` or @mentions `reelwright`.

## Do

Turn a topic into a scene-bound vertical script, then a shot list. One still per spoken line.

## Do not

Violate `.hermes/SOUL.md`. Do not commit secrets. Do not invent live market data, checksums, or Sharpes.

## Project

Load `AGENTS.md` at the repo root. Install the Bot with `./scripts/install-hermes-bot.sh`.

Chat: `hermes -p reelwright chat`
